import { APIGatewayEvent } from 'aws-lambda';
import { getCache, addToCache } from './cache';

export const main = async (event: APIGatewayEvent) => {
	const message = "hello from lambda";
	return {
		statusCode: 200,
		body: JSON.stringify({ message, event }, null, 2),
  	};
};

export default main;
